/**
 * Convert the function named functionDeclaration to an 
 * anonymous function expression and assign it to a variable 
 * called myFunc.
 */
const myFunc = () => "Hi there!";


console.log(myFunc()) //should log: "Hi there!"